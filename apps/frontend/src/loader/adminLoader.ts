/*
import { redirect } from 'react-router-dom';
import { useAuthenticatedUserStore } from '@/Stores/authenticatedUserStore.ts';

export default function adminLoader() {
    const { isUserAnAdmin } = useAuthenticatedUserStore.getState();

    if (isUserAnAdmin()) {
        return null;
    }
    return redirect('/');
}
*/
