meta {
  name: GetAll Role Permissions
  type: http
  seq: 3
}

get {
  url: {{protocol}}://{{host}}/ui/admin/role/:roleId/permission/all
  body: none
  auth: bearer
}

params:path {
  roleId: 4
}

auth:bearer {
  token: {{auth_jwt}}
}

body:json {
  [
  
  ]
}
