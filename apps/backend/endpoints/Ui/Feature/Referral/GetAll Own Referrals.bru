meta {
  name: GetAll Own Referrals
  type: http
  seq: 5
}

get {
  url: {{protocol}}://{{host}}/ui/user/:userid/services/referrals/all
  body: none
  auth: bearer
}

params:path {
  userid: self
}

auth:bearer {
  token: {{auth_jwt}}
}
