meta {
  name: Edit Own Referral
  type: http
  seq: 3
}

put {
  url: {{protocol}}://{{host}}/ui/user/:userId/services/referrals/:code
  body: json
  auth: bearer
}

params:path {
  code: UovRQ3H1
  userId: self
}

auth:bearer {
  token: {{auth_jwt}}
}

body:json {
  {
    "url": "https://test.de",
    "label": "test"
  }
}
